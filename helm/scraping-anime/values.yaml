replicaCount: 1

image:
  repository: facevedo74/scraping-anime
  tag: "105a2b0a469f0435ffa79133cb36703238a3f0ea"
  pullPolicy: Always

service:
  type: LoadBalancer
  port: 80
  targetPort: 3000
  loadBalancerIP: "172.202.10.115"

env:
  PORT: "3000"

resources:
  requests:
    cpu: "10m"
    memory: "32Mi"
  limits:
    cpu: "100m"
    memory: "64Mi"

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 90

probes:
  readiness:
    path: /
    initialDelaySeconds: 5
    periodSeconds: 10
  liveness:
    path: /
    initialDelaySeconds: 10
    periodSeconds: 20
