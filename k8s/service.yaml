apiVersion: v1
kind: Service
metadata:
  name: scraping-anime
  labels:
    app: scraping-anime
spec:
  selector:
    app: scraping-anime
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
